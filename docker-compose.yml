services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      PYTHONPATH: /app/src
      DATABASE_PATH: ${DATABASE_PATH:-/app/data/bot.db}
